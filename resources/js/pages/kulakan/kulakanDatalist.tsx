import { Head } from "@inertiajs/react";
import AppLayout from "@/layouts/app-layout";

export default function KulakanDatalist() {
    return(
        <AppLayout>
            <Head title={'Riwayat kulakan hari ini'}></Head>
        </AppLayout>
    )
}